#include "BrotliTest.hpp"
#include <drogon/utils/Utilities.h>

namespace drogon
{
namespace utils
{

std::vector<unsigned char> brotliCompress(const char* data, size_t length)
{
    return brotliCompress(data, length);
}

std::string brotliDecompress(const unsigned char* data, size_t length)
{
    return brotliDecompress(data, length);
}

} // namespace utils
} // namespace drogon